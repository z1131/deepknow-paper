# 当前开发任务

**目标**: 完成 Phase 1 (选题策划) - 子任务 1 (项目启动与状态管理)

## 进度追踪

### 1. 后端开发 (Backend) [DONE]
- [x] 设计 `project` 表并执行 SQL 建表。
- [x] 创建 `Project` 实体、Mapper、DTO。
- [x] 实现 `ProjectAppService` 和 `ProjectController`。
- [x] **修复**: 解决 Maven 依赖版本缺失问题。
- [x] **修复**: 解决 Nacos 配置加载失败问题 (Switch to Bootstrap)。

### 2. 前端开发 (Frontend) [IN PROGRESS]
- [ ] **ProjectService**: 封装 axios 调用后端 `/api/core/v1/projects` 接口。
- [ ] **状态管理**: 使用 Zustand 创建 `useProjectStore`。
- [ ] **页面骨架**: 创建 `WritingWorkspace` 页面组件。
- [ ] **路由逻辑**: 在 `App.tsx` 中配置路由，实现 `/workspace` 到 `/workspace/:id` 的跳转。

### 3. 验证与测试 [PENDING]
- [ ] 本地运行前端，联调远程后端接口。
- [ ] 验证“点击开始写作 -> 创建项目 -> 跳转”流程。

---
**下一步**: 完成前端开发后，进入 **子任务 2: AI 选题引擎**。