# 系统架构快照

## 当前架构状态
**最后更新**: 2025-12-02
**版本**: v0.2.0 (Phase 1 - Sub-task 1)

## 技术栈状态
- ✅ **前端**: React 18 + TypeScript + Vite + Tailwind CSS
- ✅ **后端**: Java 21 + Spring Boot 3.2 + Dubbo 3.2 + MyBatis-Plus
- ✅ **基础设施**: Nacos 2.3 (Config/Discovery), MySQL 8.3, Aliyun ECS x 2
- ✅ **部署**: GitHub Actions -> Aliyun ACR -> Docker Compose

## 已实现功能模块
- [x] **基础设施**: CI/CD 流水线，双机微服务部署，JVM 内存调优。
- [x] **后端骨架**: `dw-api`, `dw-core`, `dw-auth`, `dw-gateway`, `dw-common` 模块划分。
- [x] **项目管理 (API)**: `Project` 实体 CRUD，项目初始化接口。

## 进行中的模块
- [ ] **Phase 1: 选题策划**:
    - [x] 子任务 1: 项目启动与状态管理 (后端完成，前端进行中)
    - [ ] 子任务 2: AI 选题引擎
    - [ ] 子任务 3: 选题交互闭环

## 系统健康状态
- **构建状态**: ✅ 后端 CI/CD 修复通过 (Maven Build Success)
- **服务状态**: ✅ ECS A (Gateway, Auth, Nacos, Nginx) 和 ECS B (Core) 全部 Up。
- **部署状态**: ✅ 已部署到生产环境 (Aliyun ECS)。

## 关键配置变更
- **Nacos 配置**: 回滚到 `bootstrap.yml` 模式，放弃 `config.import`。
- **JVM 参数**: 设置了严格的 `mem_limit` 和 `-Xmx` 以适应 4GB 内存服务器。