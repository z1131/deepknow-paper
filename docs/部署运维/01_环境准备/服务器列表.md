# 服务器资产列表

| 节点名称 | 公网 IP (Public) | 内网 IP (Private) | SSH 用户 | 部署用途 |
| :--- | :--- | :--- | :--- | :--- |
| **ECS A** | `47.114.90.156` | `(待填)` | `root` | 控制面 (Nacos, Gateway, Auth, Nginx) |
| **ECS B** | `47.99.32.144` | `(待填)` | `root` | 数据面 (Core, RocketMQ) |

## 访问方式
- **SSH**: 使用 `ai_agent_key` 私钥。
- **Alias**: 本地已配置 `ssh ecs-a` 和 `ssh ecs-b`。
